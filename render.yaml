services:
  - type: web
    name: my-python-app
    runtime: docker
    repo: https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПО.git  # временно оставьте как есть
    plan: free
    envVars:
      - key: PORT
        value: 5000